# Session handoff — 2026-06-13

Session-close consolidated handoff covering three parallel work streams run as isolated worktree agents on 2026-06-13. Founder ratified the close.

## Git state — three feature branches preserved

All three pushed to origin. Main is **`40060d8`** "perf(map): kill 60Hz MapPage re-render under keyboard nav" — unchanged across the session.

| Branch | Tip on origin | Status |
|---|---|---|
| `feat/signature-v2` | `6ccfba6` "docs(signature-v2): add STEP 4 + STEP 5 standalone PASS screenshots" | **CONSERVED — next-session entry point.** Three.js CustomLayer works (MapLibre v5 matrix unwrap fix), geometry pipeline correct, click/hover/SidePanel work. Facade shaders do NOT visually apply on the preview. Deferred — do not grind on this further this round. |
| `feat/backlog-batch-2` | `d36ecd4` "chore: trigger preview rebuild after env scope (feat/backlog-batch-2)" | **Awaits founder visual review.** Preview Ready. 5 backlog items shipped in one commit (`e27bb84`). **Do NOT merge now.** |
| `research/tile-pipeline-v2` | `8d10ce6` "docs(tile-pipeline-v2): full design proposal + session handoff" | **Awaits founder review.** Design doc only — no code. 8 founder clarification questions queued at the end of the doc. **Do NOT proceed with rebuild without explicit founder go.** |

## Per-branch context + next step

### `feat/signature-v2` — Three.js Signature

**What works (verified locally with chromium-headless screenshots in `docs/research/signature-v2-shots/`):**
- STEP 0: official MapLibre Three.js CustomLayer example renders. Root cause was the **MapLibre v4→v5 API breaking change**: `render(gl, args)` now passes an object with `defaultProjectionData.mainMatrix`, not a flat `number[16]`. The old code's `new THREE.Matrix4().fromArray(matrix as ArrayLike<number>)` produced NaN → silent no-render. None of the six §10 hypotheses would have helped — all were premised on a valid matrix arriving.
- 1 real Majan plot 6457940 through `emitSignatureTiers` — geometry correct.
- 5 real plot shape classes (rectangular / narrow / L-shape / tiny / huge) — geometry correct.
- 123 buildings flat-shaded at realistic Dubai distribution — pipeline correct.
- Click → SidePanel opens with correct parcel — interaction parity preserved.

**What does NOT work on the deployed preview:**
- Per-category procedural facade shaders (residential / commercial / industrial / mixed / hotel / educational / healthcare / agricultural / investment) do not visually apply. Local standalone screenshots PASS for the 3×3 grid; deployed preview at `https://zaahi-j0b3ft8o3-zaahiplots-projects.vercel.app/parcels/map?render=three` does not show shader detail.

**Open decisions for founder when the next session resumes Signature work:**
- The single unresolved item is shader application on the deployed CustomLayer. Geometry, matrix, click/hover/SidePanel, and 9-shader-grid-on-standalone are all confirmed working.
- The standalone uses module-level `ShaderMaterial` per profile; the production path may need a different uniform-injection or RawShaderMaterial route. The §10 protocol's hypothesis #2 (`ShaderMaterial` → `RawShaderMaterial`) is the natural starting point.
- DATABASE_URL / DIRECT_URL on the Preview scope of `feat/signature-v2` may have come from local `.env.local` per memory `reference_db_envs`; verify against prod values before any prod work.

**Hard constraint:** do not touch `feat/signature-realistic` (the prior conservation branch). It remains the cherry-pick source if any reset is needed.

### `feat/backlog-batch-2` — 5 backlog items

All five items shipped in `e27bb84`. Preview Ready at `https://zaahi-4x6s275or-zaahiplots-projects.vercel.app`.

1. **HOVER** → gold `#C8A96E` contour-only on ZAAHI / DDA / AD. The three hover popup card JSX blocks were removed. Card opens only on click.
2. **PARCEL CARD** → new `StatusBadge` (ParcelStatus or DDA physical status). Floors row only when `plan.maxFloors > 0` — no `—` placeholder.
3. **SEARCH** → ZAAHI first → falls through to `map.querySourceFeatures` across DDA / AD-ADM / AD-OTHER. PMTiles match flies to centroid (no panel). Viewport-bounded; honest "Plot not found in viewport" error on miss.
4. **AREA 1:1** → `formatSourcePrecise()` across `area-unit.ts`, feasibility, PDF, FeasibilityV6, hover-card sqm→sqft. No more `.toFixed(0)` or `Math.round()` on area fields.
5. **SOUNDS** → `sound.uiClick()` (900 Hz triangle, 50 ms, gain 0.12) on primary CTAs; `sound.uiTap()` (440 Hz sine, 50 ms, gain 0.10) on neutral chrome. Master sound toggle respected.

**Intentionally deferred:** server-side global plot-number index (item 13 of the backlog). Viewport-bounded search ships now; the global index is a next-iteration decision once the founder sees how the viewport-only flavour feels in practice.

**Next step:** founder visual smoke on the preview URL. Merge to `main` is STOP-gated (Autonomy v2 STOP #1).

### `research/tile-pipeline-v2` — design doc only

**Doc:** `docs/research/tile-pipeline-v2-DESIGN.md` (~42 KB, no code touched). Branch tip `8d10ce6` on origin.

**6 sections summarised:**

- **§1 Inventory** — DDA tiles ship `plotNumber / mainLandUse / areaSqm (rounded) / landUse / status` but drop `emirate / district / community / developer / project_name / GFA_sqft / raw setbacks`. AD tiles include `district / community / municipality` but still drop `emirate`, status is raw, areas rounded. Oman is an orphan (R2 object + 3 build scripts; zero `src/` consumers).
- **§2 Target schema** — 7 mandatory props: `plot_number, plot_id, emirate_id, district_id, community_id, area_sqm/sqft (1:1 no rounding), land_use (10-cat enum), physical_status (5-bucket enum pre-mapped at build time)`. Three per-emirate files: `dubai-land.pmtiles`, `abu_dhabi-land-adm.pmtiles`, `abu_dhabi-land-other.pmtiles`. DDA `community_id` via point-in-polygon against `Community__1_.kml` at build time (port the drone-fps aggregate code).
- **§3 Pipeline** — single `pnpm tiles:build`. Tippecanoe writes directly to `.pmtiles` (never `.new`), `--maximum-zoom=18` locked. Verification = `xxd -l 8` magic check + `tippecanoe-decode` of 5 named plots per file + feature-count assertions + founder pause before upload.
- **§4 Search index** — recommends Option A: per-emirate static JSON on R2 (~10 MB gzipped each), lazy-loaded on search-input focus, O(1) lookup. Rejects API-hop (Vercel cold start + sovereignty hit) and `querySourceFeatures` (viewport-bounded = dishonest).
- **§5 Migration** — 5 stages. Feature flag `NEXT_PUBLIC_USE_TILES_V2`. Rollback = `vercel env rm NEXT_PUBLIC_USE_TILES_V2` (one flip, no rebuild). v1 deletion is final stage, STOP-gated.
- **§6 Cleanup** — Oman R2 delete STOP-gated. Three scripts edited (~−115 lines): `upload-tiles-r2.sh`, `update-tiles.sh`, `prepare-tiles.ts`. `scripts/rebuild-tiles-z18.sh` marked deprecated.

**8 founder clarification questions** at the end of the doc:
1. Property naming: `snake_case` (doc proposal) vs `camelCase` (matches existing page.tsx consumer)?
2. DDA `district_id`: `null` vs equal to `community_id`?
3. AD has no SUSPENDED equivalent: `null` or new `NOT_APPLICABLE` bucket?
4. AD `area_sqft` is computed (`×10.7639104167`) — acceptable under "1:1, no rounding"?
5. Search-index default emirate: Dubai eager, Abu Dhabi prefetched? Or both eager?
6. Oman R2 object delete (`oman-land.pmtiles`) — approve?
7. v1 R2 safety copy (server-side `cp tiles/dda-land.pmtiles → tiles/v1/dda-land.pmtiles`) — approve?
8. Schedule: full §3 pipeline run, or dry-run on Dubai only first?

**Data correction surfaced during inventory** (not blocking, but worth a memory update): the auto-memory entry `project_pmtiles_overzoom_band.md` says `--maximum-zoom=16` (z16→z18 overzoom band). Actual repo state since commit `68c364a` (2026-05-24) is **z18 / z18 paired, zero overzoom**. The design doc treats z18/z18 as canonical. Memory entry should be updated when convenient.

**Next step:** founder reads the doc, answers the 8 clarifications, decides on rebuild scope. Do not proceed with any R2 mutation or tile rebuild until then.

## Live processes — ALL KILLED

- No python http servers running. Port 8088 confirmed free.
- No Vercel deploy pollers active. All three worktree-agent pollers auto-completed when their respective deploys hit Ready.
- No dev servers, no transient tooling, no shells holding state.
- `/tmp` artefacts (scratch-http.log, ff-zaahi-profile, shoot-runner, step*.png, page-tsx.patch, envpull, old-three-layer.ts copies) — all removed by the agents before they returned.
- `$HOME/.nvm` persists from a prior session (cheap; future sessions reuse via `. $NVM_DIR/nvm.sh && nvm use 22`).
- `~/.cache/ms-playwright/` persists from the signature-v2 agent (cheap; future sessions reuse).
- Three agent worktrees under `/home/zaahi/zaahi/.claude/worktrees/agent-*` will be cleaned up by the harness on next session start. The branches themselves are on origin, independent of the worktrees.

## Standalone artefacts still preserved outside the repo

- `/home/zaahi/scratch/signature-realistic/maplibre-customlayer-test.html` — the cross-iteration reproducer (still valid).
- `/home/zaahi/scratch/signature-realistic/precision-test.html` — Card A / Card B precision visual.
- `/home/zaahi/scratch/signature-realistic/index.html` — original Phase A prototype with 5 plot footprints + per-category shaders.
- `/home/zaahi/scratch/signature-realistic/footprints.json` — 5 real plot rings.
- The signature-v2 agent also dropped a NEW STEP 0 reproducer somewhere in `/home/zaahi/scratch/` (see `docs/research/signature-v2-shots/` on `feat/signature-v2` for the path it screenshot from). That file is the working `add-3d-model-with-threejs` baseline + matrix unwrap fix; future Signature work should resume from it.

## Quick re-orient for next session

1. Read this file (you're here).
2. Read `CLAUDE.md` (whatever sections are loaded; AUTONOMY v2 and SESSION HANDOFF live on the `feat/signature-realistic` branch — on main they're not yet merged).
3. `git fetch origin` then `git log --oneline origin/feat/signature-v2 origin/feat/backlog-batch-2 origin/research/tile-pipeline-v2` to see the three flight-of-work tips.
4. Pick which stream founder wants to resume:
   - **Signature v2 (next-session entry point per founder)**: checkout `feat/signature-v2`, read its own `docs/research/handoff-2026-06-13.md` and `docs/research/signature-v2-shots/`. The single unresolved item is shader application on the deployed CustomLayer; geometry / matrix / cards confirmed working. §10 hypothesis #2 (`ShaderMaterial` → `RawShaderMaterial`) is the natural starting point.
   - **Backlog batch 2**: founder visual smoke → merge call.
   - **Tile pipeline v2**: founder answers 8 clarification questions → decide on rebuild scope.
