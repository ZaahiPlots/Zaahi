# Session handoff — 2026-06-12 (backlog-batch-2)

This file overwrites the prior handoff (Signature Three.js investigation) per
the "one file per date" rule. The signature investigation context is preserved
in `docs/research/signature-realistic-2026-06-10.md` §10 + the chain of
commits on `feat/signature-realistic` already pushed to origin (3afe0d5).

## Git state

- **Branch:** `feat/backlog-batch-2` (active, NOT merged)
- **HEAD:** `e27bb84` "feat(backlog-batch-2): hover gold outline · status+floors in card · PMTiles search · area 1:1 · UI sounds"
- **origin/feat/backlog-batch-2:** to be pushed (autonomy v2 allows feature-branch push without founder)
- **origin/feat/signature-realistic:** untouched, still at `3afe0d5` (parallel session frozen)
- **main:** `40060d8` — prod tip, untouched by this branch.

## Done in session

Five founder-backlog items shipped on `feat/backlog-batch-2`:

1. **#9 Hover gold outline only (no card).** Added `ZAAHI_PLOTS_HOVER_LINE`
   layer + per-PMTiles `${fillId}-hover` line layers, both paint brand gold
   `#C8A96E`. mousemove flips `setFilter`; mouseleave clears. The three
   hover popup cards (zaahiHover / vaultHover / ddaLandHover JSX blocks)
   were removed — full SidePanel still opens on click. Hover state vars
   kept alive so click → flyTo + setSelectedParcelId keeps working.

2. **#11 Status badge + #10 Floors row in SidePanel.** `StatusBadge`
   component colour-codes ParcelStatus enum (LISTED green, IN_DEAL gold,
   SOLD grey, VAULT_PRIVATE gold-dim, DISPUTED red) + DDA physical
   statuses (Completed, Under_Construction). Floors row appears next to
   FAR/Plot Area when `plan.maxFloors > 0`; DDA/AD tile-sourced features
   don't have floors so they get no row at all (no `—` filler per spec).

3. **#13 Search across all plots.** HeaderBar `doFind` now: probes
   `/api/parcels/map` for ZAAHI 132 listings → falls through to
   `map.querySourceFeatures` on 3 PMTiles sources (dda-land, ad-adm,
   ad-other). PMTiles match → flyTo centroid only (no SidePanel since
   no DB row). Surface explicit error "Plot not found in viewport —
   pan/zoom to area" when no match. Server-side global index = next
   iteration item (deferred — would need ~30K row plot_number → centroid
   server-side cache, or proxy to DDA/TAMM endpoints).

4. **#7 Area 1:1 with source, no rounding.** Three rounding callsites
   removed:
   - `src/lib/area-unit.ts`: `Math.round(sqft|sqm).toLocaleString()` →
     `formatSourcePrecise()` using `toLocaleString("en-US", { maximumFractionDigits: 20 })`.
   - `src/lib/feasibility.ts` and `src/lib/generate-site-plan-pdf.ts`:
     `fmtInt` reworked to preserve decimals while still grouping.
   - `src/components/feasibility/FeasibilityV6Calculator.tsx`: dropped
     `Math.round(area.gfa)` / `Math.round(area.sfa)` on readonly
     NumberInput display values (BUA Math.round in setter kept —
     working-state integer override).
   - PMTiles hover-card sqm→sqft conversion dropped Math.round.

5. **#33 Netflix-style button sounds.** Added `sound.uiClick()` (900 Hz
   triangle, 50 ms, gain 0.12) for primary CTAs and `sound.uiTap()`
   (440 Hz sine, 50 ms, gain 0.10) for neutral chrome. Selectively wired:
   - HeaderBar Find submit, FilterPanel Reset, SidePanel Start Negotiation
     + Download Site Plan, rail 2D/3D toggle → uiClick.
   - Rail Layers, Legend, Zoom+, Zoom− → uiTap.
   Master sound toggle continues to mute everything via the master gain.

## Invariants preserved (page.tsx memory rule)

- `fill-extrusion-opacity` literal numbers (ZAAHI 1.0, PMTiles 0.35) — UNCHANGED
- `ZAAHI_LANDUSE_COLOR` 10 categories — UNCHANGED
- `loadZaahiPlots` + `computeSetbackM` + `insetRingByMeters` +
  `scaleRingFromCentroid` (3-tier podium/body/crown) — UNCHANGED
- `?render=three` flag logic — UNCHANGED (parallel session owns it)
- `src/lib/signature/*` — UNTOUCHED
- Single-source single-layer ZAAHI 3D architecture — UNCHANGED
- Auth flow + SECURITY RULES + AGENT RULES — UNCHANGED
- `bindLayerEvent` listener-leak guard — UNCHANGED

## In progress now

- Push branch to origin and trigger Vercel preview.
- Watch preview deploy with a poller; kill after Ready.

## Next step (when founder reviews)

Founder smoke checklist (per backlog item):

1. **Hover:** cursor on any ZAAHI listing, DDA plot, AD plot → contour
   lights up in brand gold; NO card pops up. Click → SidePanel opens.
2. **Status + Floors:** click any LISTED parcel → header shows green
   "LISTED" pill next to plot number; dimensions section shows "Floors:
   N" when DDA/AffectionPlan has the value. Click a DDA/AD background
   plot (if added to vault) → status pill shows the DDA construction
   status; Floors row absent if no data.
3. **Search:** type a plot number from a non-listed DDA plot visible in
   viewport → camera flies to it (no panel — expected). Type a plot
   number NOT in viewport → "Plot not found in viewport" error.
4. **Area 1:1:** click any listing → Plot Area displays whatever DDA
   carries verbatim (e.g. `47,250.83 sqft` if DDA has 2 dp; `33,862 sqft`
   if whole-number source). No silent rounding to the nearest 1.
5. **Sounds:** master sound toggle ON, click any rail Layers / Zoom
   button → subtle 50 ms blip. Click HeaderBar search submit → brighter
   blip. Click SidePanel "Start Negotiation" → brighter blip. Toggle
   sound OFF → all silent.

## Open decisions (waiting on founder)

- **Merge `feat/backlog-batch-2` → `main`** — STOP #1, explicit founder
  OK required after preview verification.
- **Server-side global plot search** — backlog #13's next iteration.
  Two paths: (a) build static `plot_number → centroid` JSON during tile
  build, serve via `/api/search/plot/[plotNumber]`; (b) proxy DDA/TAMM
  lookup APIs server-side. Founder picks once viewport-bound search
  proves out in production.

## Live processes

- **No pollers active** at time of writing this handoff. After push, will
  start ONE poller watching the new preview deploy.
- **No dev servers running.**
- **No transient tooling.** Node 22 from prior session's `$HOME/.nvm`
  install persists but is dormant (only activated inside specific shells).
- **No temp files in /tmp.**

## Known bugs + hypotheses

- None active in this batch. The Signature Three.js bug (flat-grey /
  silent install failure) is FROZEN on `feat/signature-realistic`, not
  affected by this work. See `signature-realistic-2026-06-10.md` §10 if
  founder resumes that thread.

## Uncommitted files

- **This handoff doc** (`docs/research/handoff-2026-06-12.md`) — about
  to be committed on `feat/backlog-batch-2`.

## Quick re-orient for next session

1. Read this file (you're here).
2. Read `docs/research/founder-backlog-audit-2026-06-10.md` for the
   33-item backlog context.
3. Read CLAUDE.md (loaded automatically).
4. Confirm branch state: `git log --oneline feat/backlog-batch-2 | head -5`.
   Expected tip is `e27bb84` (or +1 for this handoff commit).
5. If founder approves: open a PR from `feat/backlog-batch-2` → `main`,
   wait for visual verification, merge. No further code work needed.
6. If founder requests changes: branch is open for amends; do NOT
   force-push if there's anything else on origin.
