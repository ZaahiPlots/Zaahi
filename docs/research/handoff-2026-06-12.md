# Session handoff — 2026-06-12 (research/tile-pipeline-v2)

Live working file for this date / branch. Overwrite in-place within
the day; do not branch off.

> **Note:** another branch (`feat/signature-realistic`) already has a
> different `handoff-2026-06-12.md` for that session's STOP. This
> file is the parallel one for the v2 tile-pipeline design work.
> Each branch keeps its own handoff because both branches need
> independent cold-starts.

## Git state

- **Branch:** `research/tile-pipeline-v2` (created from `origin/main`)
- **HEAD (local):** to-be-recorded after this commit
- **origin/research/tile-pipeline-v2:** pushed at end of session (per АВТОНОМИЯ v2 — feature-branch push is autonomous, not main)
- **main:** `40060d8` "perf(map): kill 60Hz MapPage re-render under keyboard nav" (untouched)

## Done in session

- Cold-start: read `feat/signature-realistic` handoff (no overlap), pointer files `feedback_pmtiles_verification`, `feedback_no_credential_commands`, `project_pmtiles_overzoom_band`; CLAUDE.md sections АВТОНОМИЯ v2 + SECURITY RULES + PMTiles section; scanned `scripts/{prepare-tiles.ts,update-tiles.sh,upload-tiles-r2.sh,rebuild-tiles-z18.sh,fetch-{dda,ad,oman}-plots.ts}`; `src/app/parcels/map/page.tsx` (`addLandTileSource`, hover popup state, `searchPlot` API); `docs/r2-migration-plan.md`; `docs/research/dda-bulk-refresh.md`; `BACKLOG.md` (update-tiles + Oman items); `/home/zaahi/scratch/drone-fps/aggregate-status-by-community.mjs` (physical-status vocab).
- Wrote `docs/research/tile-pipeline-v2-DESIGN.md` — six sections per founder spec:
  - §1 Inventory: DDA, AD-ADM, AD-Other, data.dubai, Community KML, Oman (orphan); drift table.
  - §2 Target schema: 7 mandatory + 4 geometry + ~9 optional properties, snake_case proposal, per-emirate file layout. 7 open questions flagged for founder.
  - §3 Pipeline: single `pnpm tiles:build` command; 6 steps; tippecanoe writes directly to `.pmtiles` (no `.new`); `--maximum-zoom=18` locked; `xxd` magic + 5-plot decode verification gates the upload.
  - §4 Search index: recommended Option A — sharded static JSON on R2 (~10 MB gzipped per emirate), O(1) lookup, lazy-loaded on search focus.
  - §5 Migration: 5 stages; rollback is `vercel env rm NEXT_PUBLIC_USE_TILES_V2` (one flag flip); R2 deletes gated behind founder STOP.
  - §6 Cleanup: Oman script-orphans documented (3 scripts, ~−115 lines), `data/` kept per CLAUDE.md, R2 delete STOP-gated.
- Captured one inventory-time data-correction worth surfacing: the conversation header summary mentioned `--maximum-zoom=16` and a "z16→z18 overzoom band"; the actual repo state (`update-tiles.sh` line 103, `addLandTileSource` source line 4087, `page.tsx` map maxZoom=18) is **z18 / z18 paired, no overzoom**. The 16→18 setup ended on 2026-05-24 with commit `68c364a`. The design doc treats z18/z18 as the canonical contract and references `project_pmtiles_overzoom_band` memory.

## Status — DESIGN-DOC ONLY DELIVERED

Branch is design-only by construction. STOP at the end of §6 for
founder review. No further work without explicit answers to the 8
questions at the bottom of the design doc.

## Next step (when founder responds)

1. Read answers to Q1–Q7 + Oman delete approve + v1 copy approve + schedule.
2. Cut a `feat/tiles-v2` branch (separate from this research branch).
3. Execute §3 pipeline on a build box: fetch → normalise → tippecanoe → verify → R2 upload (versioned paths).
4. Per `feedback_pmtiles_verification`: write directly to `.pmtiles`, `xxd` magic, decode 5 named plots per file, pause for founder approval **before** uploading to R2.
5. Then the `feat/tiles-v2` page.tsx edit, env var on Preview, founder smoke, merge gate (STOP #1).

## Open decisions (waiting on founder)

All 8 in the design doc's "STOP — Founder review gate" section:
1. snake_case vs camelCase property convention
2. DDA `district_id` null vs equal to `community_id`
3. AD SUSPENDED bucket null vs `NOT_APPLICABLE`
4. AD computed sqft (×10.7639104167) as "1:1"
5. Search-index default emirate (Dubai eager, AD prefetched?)
6. Oman R2 delete approval
7. v1 R2 copy approval
8. Schedule (full run vs Dubai-only dry run)

## Live processes — NONE

- No fetchers running. No tippecanoe ever invoked in this session.
- No R2 mutations. No env vars touched.
- No dev servers. No transient tooling. No background processes.

## Known bugs + hypotheses

- N/A — design-doc only, nothing built.

## Uncommitted files

- `docs/research/tile-pipeline-v2-DESIGN.md` — about to be committed.
- `docs/research/handoff-2026-06-12.md` — this file, about to be committed.
- No other touched files (`git status` confirmed clean before write).

## Quick re-orient for next session

1. Read this file (you're here).
2. Read `docs/research/tile-pipeline-v2-DESIGN.md` end-to-end — 8
   questions at the bottom drive the next action.
3. Read CLAUDE.md (auto-loaded) — especially АВТОНОМИЯ v2 STOP #3
   (R2 delete) and the PMTiles serving section.
4. Check the parallel `feat/signature-realistic` handoff for any
   updates — different work stream, no overlap, but useful to
   confirm prod tip hasn't moved.
5. If founder approves go-ahead, branch `feat/tiles-v2` from `main`
   (don't re-use this research branch) and start §3 pipeline.
