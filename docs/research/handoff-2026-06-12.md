# Session handoff — 2026-06-12

Live working file. Overwrite in-place within the day; do not branch off.

## Git state

- **Branch:** `feat/signature-realistic` (active)
- **HEAD (local):** `3afe0d5` "fix(map): defensive try/catch + WebGL context-lost/restored handlers"
- **origin/feat/signature-realistic:** synced — `3afe0d5` pushed
- Also held local (not pushed yet): `a59c29f` "docs(claude): ratify SESSION HANDOFF protocol + today's handoff (2026-06-12)" — was held to avoid superseding the `zaahi-cucsaeit2` deploy; superseded anyway by 3afe0d5. Safe to push together with next fix.

Wait — actually `a59c29f` was committed BEFORE `923fe7f`'s deploy `zaahi-cucsaeit2` came back, but `3afe0d5` was pushed AFTER. The git linearisation: 746cc74 → 7562866 → efaa9aa → 1c47133 → 1deb78e → 5aced17 → 96d424a → 6cbec8b → 923fe7f → **a59c29f** → 3afe0d5. So a59c29f is in the pushed chain ahead of 3afe0d5. Confirmed via git log.

- **main:** `40060d8` "perf(map): kill 60Hz MapPage re-render under keyboard nav" — last prod tip (no Signature work merged yet).

History on `feat/signature-realistic` (newest first):

```
3afe0d5 fix(map): defensive try/catch + WebGL context-lost/restored handlers
a59c29f docs(claude): ratify SESSION HANDOFF protocol + today's handoff (2026-06-12)
923fe7f fix(map): per-mesh local origin — Stage 5 flat-grey root cause (precision overflow)
6cbec8b fix(map): force linear-srgb output on Three.js renderer (flat-grey bug attempt #1)
96d424a debug(map): three-layer setBuildings/setSelected console diagnostics
5aced17 feat(map): STAGE 4 + STAGE 5 — full category coverage + parity + LOD
1deb78e docs(claude): ratify АВТОНОМИЯ v2 (2026-06-12, replaces 2026-05-23 rule)
1c47133 chore: trigger preview rebuild after env-scope + R2 CORS fix
efaa9aa feat(map): STAGE 3 residential / commercial / industrial facade shaders
7562866 feat(map): STAGE 2 Three.js CustomLayer ?render=three
746cc74 refactor(map): STAGE 1 src/lib/signature/geometry.ts + parity script
```

Most recent Vercel preview deployment: `zaahi-htioll2us-zaahiplots-projects.vercel.app` (status at update: **Building** — poller `bdfd27juy` watching). Previous `zaahi-cucsaeit2` confirmed Ready but had "WebGL context was lost" + zero Three.js logs — root cause for the new defensive fix.

## Done in session

- **STAGE 1–5** of Signature realistic feature complete + pushed.
- **Vercel env scope:** added 4 missing vars (NEXT_PUBLIC_TILES_BASE_URL, DATABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, restored NEXT_PUBLIC_TILES_BASE_URL after accidental rm) as branch-scoped Preview entries for `feat/signature-realistic`.
- **R2 CORS** on `zaahi-tiles` bucket: merged in `http://localhost:3000` + `https://*.vercel.app` alongside existing prod origins; defensively preserved `Range` allowed-header + `Accept-Ranges` exposed-header for PMTiles HTTP-Range fetching. Verified via 3 curls.
- **CLAUDE.md:** АВТОНОМИЯ v2 ratified (2026-06-12, replaces 2026-05-23 "когда спрашивать founder").
- **CLAUDE.md:** SESSION HANDOFF protocol ratified (this file) — added 2026-06-12.

## Status — INVESTIGATION PAUSED (founder STOP, 2026-06-12)

Branch `feat/signature-realistic` is frozen as-is. Nothing merged. Prod (`main`) untouched on the old fill-extrusion path. Full Investigation log captured in `docs/research/signature-realistic-2026-06-10.md` §10 (5 iterations · suspects ruled out with receipts · last symptom · open hypotheses · standalone artifacts). Next session reads that §10 first.

The line above replaces the original "In progress now / Next step" sections — there is no in-progress work and no next step until founder picks the investigation back up.

---

## (Archive of pre-STOP state for context — historical only)

### Final iteration before STOP

5th fix attempt was `8da7e63` (pre-install gate diagnostic + live URL fallback). Deploy `zaahi-ke5cptyn0` was Building when STOP came in. Standalone test on 127.0.0.1:8088 showed the cleanest reproduction: 300 meshes added cleanly + a deliberately massive bright-red 100×100×60 m debug cube — *neither* rendered, no errors. That narrows the bug to MapLibre CustomLayer ↔ Three.js camera/projection-matrix synchronisation, not anything in the shader or the precision math (both confirmed working in earlier iterations).

### Earlier ruled-out (history)

Confirmed pattern on `zaahi-htioll2us`:
- `[ZAAHI] addLayer: zaahi-plots-buildings-3d features: 310` ✓
- `[ZAAHI] installing Three.js custom layer` ✗ (missing)
- `[ZAAHI] Three.js install/refresh failed: …` ✗ (no error caught)
- `[ZAAHI three-layer] onAdd FAILED:` ✗
- `[zaahi-plots] load failed` ✗ (outer catch not firing)

Code between `addLayer` (page.tsx:3517) and `if (useThreeRender)` (page.tsx:3550) is pure closing braces + comments — physically can't throw. Only remaining cause: `useThreeRender` evaluates **false** despite the URL containing `?render=three`.

Most-likely root cause: SSR/hydration race on `useMemo([], …)` — factory runs during SSR with `typeof window === "undefined"` → returns `false`. React reuses the memo value, and the `loadZaahiPlots` closure captures it.

Fix `8da7e63`:
1. Logs both `useThreeRender(memo)` AND `liveRenderThree` (read directly from `window.location.search`) right before the gate.
2. Live-reads URL as fallback — `installShouldRun = useThreeRender || liveRenderThree`. Install fires whenever the URL is truthy regardless of memoization race.

Once the next preview confirms `useThreeRender(memo)=false liveRenderThree=true` → promote the live read to canonical (replace useMemo with an effect-set state, or just use `liveRenderThree` everywhere).

**Earlier ruled-out (history):**

Founder's `zaahi-sj7ym01dw` (96d424a/6cbec8b): shader path active (309/310 ShaderMaterial), landUse correct, setSelected(null) clean, outputColorSpace=srgb-linear, ColorManagement enabled. Buildings rendered as flat grey #7a7a7a at z=16.5.
- Ruled out: uDesaturate stuck at 1, landUse → Lambert fallback, color-space double-encoding.
- Suspected: precision overflow in vLocal at large absolute vertex coords → `923fe7f` per-mesh origin fix.

Founder's `zaahi-cucsaeit2` (923fe7f): "WebGL context was lost" message + ZERO Three.js console logs. Fill-extrusion 310 features still rendered.
- Suspected: 15k-iteration per-vertex diagnostic loop in 923fe7f → `3afe0d5` removed it + added try/catch around install + webglcontextlost/restored handlers.

Founder's `zaahi-htioll2us` (3afe0d5): same silent fail as `zaahi-cucsaeit2`. No errors reach any catch — install gate isn't being entered.

- Founder's preview report on `zaahi-sj7ym01dw`: shader path active (309/310 meshes are ShaderMaterial), landUse arrives correctly across all 8 categories, `setSelected: null` clean, but buildings all render flat grey #7a7a7a at z=16.5.
- Founder's follow-up report on `zaahi-cucsaeit2` (after the per-mesh origin fix `923fe7f`): "WebGL context was lost" message + ZERO Three.js console logs at all. Fill-extrusion 310 features still render. So loadZaahiPlots completed (fill-extrusion addLayer succeeded) but something between L3517 and L3552 silently failed — the outer `[zaahi-plots] load failed` catch swallowed it.
- **Suspects ruled out by console logs**:
  - uDesaturate stuck at 1 — ruled out (`setSelected: null`)
  - landUse → Lambert fallback — ruled out (`shader=309 lambert=1`)
  - color-space double-encoding — ruled out (`outputColorSpace=srgb-linear` applied, no change)
- **Current hypothesis (fix #4, just deployed):** the `923fe7f` per-vertex diagnostic loop (Math.abs on every position.getX/Y across ~15k+ entries inside setBuildings) may have correlated with a GPU watchdog trip; OR the per-mesh `mesh.position.set(meshOriginXm, meshOriginYm, 0)` interacted badly with the renderer's resetState path under MapLibre's shared GL context.
- **Fix applied in `3afe0d5`:**
  1. `try/catch` wrapping page.tsx's whole `if (useThreeRender) { … }` block — silent fail no longer possible.
  2. `try/catch` on three-layer.ts `onAdd` + `setBuildings` with `console.error` prefixed errors.
  3. `webglcontextlost` / `webglcontextrestored` handlers on `map.getCanvas()`. On restored: removeLayer → dispose dead renderer → disposeGroup → re-addLayer.
  4. Dropped the per-vertex diagnostic loop (only sample first-mesh first-vertex now).

## Next step (when investigation resumes) — PROTOCOL

Founder directive 2026-06-12 (post-STOP): **rebuild from scratch first; if the cube still doesn't appear, walk the §10 hypotheses in their numbered order. Do not invent new hypotheses until all 6 are exhausted.**

1. **STEP 0** — build the official MapLibre Three.js CustomLayer example from scratch (separate new file, not the existing `maplibre-customlayer-test.html`). Single mesh, solid colour, no shader. This bisects "is the platform underneath the issue?" from "are our deltas the issue?".
2. **If STEP 0 cube renders** → re-introduce our deltas one at a time against the §10 hypotheses **in order**. Cheapest first: `renderer.resetState()` ordering (1) → `ShaderMaterial` → `RawShaderMaterial` (2) → matrix multiplication order (3) → `outputColorSpace` pinning (4) → `camera.matrixWorldInverse` staleness (5) → scene/camera `up` mismatch (6).
3. **If STEP 0 cube does NOT render** → stop the local bisect path; debug the platform / env first before continuing.

Full details + rationale per hypothesis in `docs/research/signature-realistic-2026-06-10.md` §10 "Quick re-entry guide for the next session".

## Open decisions (waiting on founder)

- **Visual approval** of Stage 4 (6 new categories — mixed-use / hotel / educational / healthcare / agricultural / investment) — blocked on flat-grey fix.
- **Merge `feat/signature-realistic` → `main`** when verified — explicit founder OK required (STOP #1).
- **SIG-FINAL** (post-merge follow-up): unify `src/lib/zaahi-3d-tiers.ts` (vault rendering) with `src/lib/signature/geometry.ts` (listings rendering) — currently use different `insetRingByMeters` algorithms; deferred decision per ratification 2026-06-11.
- **Remove the old MapLibre fill-extrusion render path** (currently default when `?render=three` is off) — separate decision once Three.js path proves out on real eyes.

## Live processes — ALL KILLED (post-STOP)

- **Python HTTP server** (PID was `260346`) — `kill 260346` ran; port 8088 confirmed free.
- **Poller `bgkvbvr8o`** — TaskStop'd. Was watching `zaahi-ke5cptyn0`.
- **All other pollers** (`b6lwdu1ji`, `bm3je18av`, `bxt0ax17o`, `b8og82v3s`, `b8sib6wfu`, `bi6cfgy1o`, `bdfd27juy`) — already stopped earlier in the session.
- **Temp dirs** — `/tmp/ff-zaahi-profile`, `/tmp/zaahi-standalone.png`, `/tmp/scratch-http.log` removed.
- **No dev servers, no transient tooling, no shells holding state.**
- Cached nvm install at `$HOME/.nvm` persists (cheap; future sessions can re-source it with `. $NVM_DIR/nvm.sh && nvm use 22`).
- **No dev servers running.** `pkill -f next-server 2>/dev/null` confirmed earlier in session.
- **Transient Node 22** via nvm in `$HOME/.nvm` — only active inside specific shell invocations that do `. $NVM_DIR/nvm.sh && nvm use 22`. System Node 20.20.2 unchanged. Wrangler installable via `npx --yes wrangler@latest` under that env.
- **R2 wrangler OAuth** alive (`zhanrysbayev@gmail.com` account `1ded8bed02...`). No need to re-auth.

## Known bugs + hypotheses

### Flat-grey Three.js shader render (active, fix #3 in preview)

- Symptom: all buildings render flat `#7a7a7a` grey on `?render=three` at z=16.5 in Majan / Dubai parcels.
- Ruled out: uDesaturate (was 0), landUse mapping (309/310 shader meshes confirmed), Three.js color-space double-encoding (linear-srgb output didn't help).
- Current hypothesis: vLocal precision overflow at large absolute vertex coords (project-meters from anchor at 55.27/25.20).
- Fix attempt `923fe7f` (deployed): per-mesh origin subtraction; geometry vertices in ±50 m band; `mesh.position` restores absolute placement via Three.js's modelView. Confirms via `maxVertexAbs(m)` log.
- **Fallback plan if fix #3 fails:** switch to `RawShaderMaterial` with explicit `uMeshOrigin` uniform; possibly Three.js's auto-injected `modelViewMatrix` is the interfering factor.

### Standalone test artifacts (outside repo)

- `/home/zaahi/scratch/signature-realistic/maplibre-customlayer-test.html` — **2026-06-12 added.** Real MapLibre map (OSM raster basemap) + Three.js CustomLayer + 300 procedural-shader meshes with per-mesh origin. On-page log box captures `console.log` / `console.error`. Most useful for reproducing context loss locally without a deploy cycle.
- `/home/zaahi/scratch/signature-realistic/precision-test.html` — 2-card Three.js sandbox demonstrating Card A (small vertex coords → textured) vs Card B (large vertex coords + offset → expected grey). Visual confirmation tool for the precision hypothesis.
- `/home/zaahi/scratch/signature-realistic/index.html` — original Stage A prototype with 5 plot footprints. Reference for shader visual targets.
- `/home/zaahi/scratch/signature-realistic/footprints.json` — 5 real plot rings used by precision-test + parity test (parity script reads it via direct path).
- `/home/zaahi/scratch/drone-fps/aggregate*.{json,mjs}` — preserved master copies from drone-fps postmortem (separate unrelated thread).

## Uncommitted files

- **This handoff doc** (`docs/research/handoff-2026-06-12.md`) — about to be committed.
- **CLAUDE.md SESSION HANDOFF section** (just added above the "Если застрял" section) — about to be committed alongside this file.
- `/home/zaahi/scratch/signature-realistic/precision-test.html` — standalone diagnostic, intentionally stays outside repo. If the founder wants it as a permanent reference, move to `docs/research/signature-realistic-shots/`.
- Many older untracked files in `docs/research/` and `scripts/` (`_diag-*`, `_smoke-*`, `archie-*-2026-06-10.md`, etc.) — pre-existing from previous sessions, not related to today's work.

## Quick re-orient for next session

1. Read this file (you're here).
2. Read `docs/research/signature-realistic-2026-06-10.md` §10 "Investigation log — STAGES 1-5" — every iteration with receipts.
3. Read `CLAUDE.md` (loaded automatically).
4. Confirm branch state: `git log --oneline feat/signature-realistic | head -15`. Expected tip is the handoff-update commit; no force-push has touched the chain.
5. If founder wants to resume: follow `signature-realistic-2026-06-10.md` §10 "Quick re-entry guide" — start the python server, open the standalone, swap `ShaderMaterial` → `RawShaderMaterial` first.
6. If founder wants to drop Plan A: discuss Plan B (hybrid lighting on the existing `fill-extrusion`) from §9 of the same doc.
